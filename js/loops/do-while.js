let signo = 'exit';

do {
    console.log(`Se ejecuto una vez`)
    // signo = prompt(`Ingrese su mes de nacimiento`);
    // signo = 'exit'

}while(signo !== 'exit');

console.log(`Salió del bucle while`)

// Pedirle al usuario que ingrese un número, y calcular desde el 1 hasta dicho número cuales son divisores del mismo